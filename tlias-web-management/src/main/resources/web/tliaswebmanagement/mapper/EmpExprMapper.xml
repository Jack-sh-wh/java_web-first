<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="web.tliaswebmanagement.mapper.EmpExprMapper">


<!--    <insert id="insertBatch">-->
<!--        insert into emp_expr (emp_id,begin,end,company,job) values-->
<!--        <foreach collection="empExprList" item="expr" open="(" close=")" separator=",">-->
<!--            #{expr.empId},#{expr.begin},#{expr.end},#{expr.company},#{expr.job}-->
<!--        </foreach>-->
<!--    </insert>-->

<!--</mapper>-->

    <insert id="insertBatch">
        insert into emp_expr (emp_id,begin,end,company,job) values
        <foreach collection="empExprList" item="expr" separator=",">
            (#{expr.empId},#{expr.begin},#{expr.end},#{expr.company},#{expr.job})
        </foreach>
    </insert>
    <delete id="delete">
        delete from emp_expr where id in
        <foreach item="id" collection="ids" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
